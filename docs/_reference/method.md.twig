```php
{{ generateSignature(method, 80) }}
```

{% if method.summary is not empty %}
##### {{ method.summary|raw }}
{% endif %}

{{ method.description|raw }}

{% if method.deprecated %}**Warning:** this method is **deprecated**.{% endif %}

{% if method.isStatic() %}| Static | | This method is **static**. |
{% endif %}
{% if method.name != '__construct' and method.response %}| Returns | **{{ method.response.type == 'self' ? node.name : method.response.type|linkClasses|replace({'|': '\\|'}) }}** | {{ method.response.description|raw }} |
{% endif %}
{% if method.tags.throws is not empty %}{% for throws in method.tags.throws %}| Throws | {{ throws.type|linkClasses }} | {{ throws.description}} |
{% endfor %}{% endif %}


{% if method.arguments is not empty %}
##### Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
{% for argument in method.arguments %}
| `${{ argument.name }}` | {% if argument.getType() ~ ' ' != 'mixed' %}**{{ argument.type|linkClasses }}**{% endif %}  | {{ argument.description|replace({'|': '&#124;'})|raw }} |
{% endfor %}

{% endif %}

{% if method.tags.see is not empty or method.tags.link is not empty %}
**See Also:**

{% for see in method.tags.see %}
* {{ see.reference|linkClasses }} {% if see.description is not empty %}- {{ see.description|raw }}{% endif %}
{% endfor %}
{% for link in method.tags.link %}
* [{{ link.description ?: link.link }}]({{ link.link }})
{% endfor %}

{% endif %}
